#ifndef MATH_H
#define MATH_H
float to_radians(float degrees);
float random_range(float minv, float maxv);


#define MAX(a,b)                                \
  a > b ? a : b

#define MIN(a, b)                               \
  a < b ? a : b

#endif
